{
  "name": "reusable-workflows",
  "private": true,
  "type": "module",
  "comment": "Root tooling is dev-only: eslint + prettier lint this repo's own .mjs and YAML. Action/script runtime stays zero-dependency (Node 24 built-ins only). The only runtime dep in the repo lives under shadow/ (yaml).",
  "scripts": {
    "lint": "eslint . && prettier --check \"**/*.{mjs,yaml,yml}\"",
    "lint:fix": "eslint . --fix && prettier --write \"**/*.{mjs,yaml,yml}\"",
    "format": "prettier --write \"**/*.{mjs,yaml,yml}\""
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-yml": "^1.16.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2"
  }
}
