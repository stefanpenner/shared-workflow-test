{
  "name": "reusable-workflows",
  "private": true,
  "type": "module",
  "comment": "Root tooling is dev-only: eslint + prettier lint this repo's own .mjs / .mts / YAML and enforce the import allowlist (node:, relative, yaml, @actions/*). Action/script runtime stays dependency-free; the only runtime dep lives under shadow/ (yaml).",
  "scripts": {
    "lint": "eslint . && prettier --check \"**/*.{mjs,mts,yaml,yml}\"",
    "lint:fix": "eslint . --fix && prettier --write \"**/*.{mjs,mts,yaml,yml}\"",
    "format": "prettier --write \"**/*.{mjs,mts,yaml,yml}\""
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-yml": "^1.16.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2",
    "typescript-eslint": "^8.18.1"
  }
}
