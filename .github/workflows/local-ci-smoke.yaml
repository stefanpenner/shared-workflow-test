name: Self-hosted smoke
on:
  workflow_dispatch:
jobs:
  smoke:
    runs-on: [self-hosted, local-cache]
    steps:
      - uses: actions/checkout@v4
      - uses: bazel-contrib/setup-bazel@0.19.0
      - name: Build the actions via the local caches (timed)
        run: bash tools/smoke.sh
