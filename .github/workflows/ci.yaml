name: Use Shared Workflow

on:
  push:
    branches: ['**'] # every branch, so WIP commits get CI (not just main / PRs)
  workflow_dispatch:

jobs:
  shared:
    uses: ./.github/workflows/shared.yaml
    with:
      ref: ${{ github.sha || '--non-existent-ref--'}}
