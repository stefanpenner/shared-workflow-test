name: Use Shared Workflow

on:
  push:
    branches: [main]
  pull_request:

jobs:
  call-shared:
    uses: ./.github/workflows/shared.yaml
    with:
      ref: ${{ github.sha || '--non-existent-ref--'}}
