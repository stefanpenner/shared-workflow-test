name: Use Shared Workflow

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  call-shared:
    uses: ./.github/workflows/shared.yml
    with:
      ref: ${{ github.sha || '--non-existent-ref--'}}
